{
  "name": "curatorlab-deploy",
  "type": "module",
  "scripts": {
    "build": "cd landing && npm install && npm run build && cd ../app && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
